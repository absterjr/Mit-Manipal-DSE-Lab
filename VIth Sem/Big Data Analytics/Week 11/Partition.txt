#1: create table if not exists hospital_part (name string, eid int, add string, Dept string, Sal string, shifts int) 
    partitioned by (department string)
    row format delimited
    fields terminated by '\t'
    lines terminated by '\n';

#2: insert overwrite table hospital_part  partition(department = 'ent') select ename, id, address, department,salary,     shift from hospital_emp where department='ent';
